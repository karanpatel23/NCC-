# NCC website implementation plan · 24 September 2026

The owner approved a full site revision and later directed that Home's design and layout remain unchanged. Home rendering is therefore retained. Its metadata and unsupported credential wording may be corrected without changing its composition.

1. Preserve the 57 source records, exact approved vision and contact/legal facts. Replace raw tender phrasing while retaining all technical identifiers, road names and dimensions. Restore verified client groups and remove PMGSY from the client list.
2. Apply an editorial engineering design to content pages only: navy, slate, copper and white; thin route lines; restrained glass cards with opaque fallbacks. Keep the existing Home composition and shared navigation appearance.
3. Remove the legacy map from About because the bundled DataMeet geometry is not verified Survey of India material. Present the project states as text instead.
4. Add route-specific canonical/social metadata, a sitemap, robots directives and sourced organisation structured data. Resolve lint, build and gallery issues.
5. Validate all 66 public routes at 360, 390, 768, 1024, 1440 and 1920 px; check interactive controls, image loading, Lighthouse and the production build. Deploy only through the existing linked Vercel account/project, then reopen production routes.

The external 21st context search was rejected by automatic approval review because it could upload repository context. The local design context is documented in `site/.21st/`; no repository context was uploaded.
